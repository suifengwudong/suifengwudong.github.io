---
const title = "Notes";
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | Suifeng Wudong</title>
    <style>
      :root {
        --bg-color: #ffffff;
        --sidebar-bg: #f8fafc;
        --text-color: #1e293b;
        --text-muted: #64748b;
        --accent-color: #0284c7;
        --border-color: #e2e8f0;
        --max-width: 900px;
      }
      body {
        font-family:
          -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        line-height: 1.6;
        margin: 0;
        padding: 0;
        background-color: var(--bg-color);
        color: var(--text-color);
      }
      .header {
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(8px);
        padding: 1rem 2rem;
        border-bottom: 1px solid var(--border-color);
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: sticky;
        top: 0;
        z-index: 100;
      }
      .logo {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--accent-color);
        text-decoration: none;
      }
      .nav-links {
        display: flex;
        gap: 2rem;
      }
      .nav-links a {
        color: var(--text-muted);
        text-decoration: none;
        font-weight: 500;
        transition: color 0.2s;
      }
      .nav-links a:hover {
        color: var(--accent-color);
      }
      .hero {
        text-align: center;
        padding: 6rem 2rem;
        background: linear-gradient(to bottom, #f8fafc, #ffffff);
        border-bottom: 1px solid var(--border-color);
      }
      .hero h1 {
        font-size: 3.5rem;
        margin: 0;
        font-weight: 800;
        background: linear-gradient(135deg, #0f172a 0%, #0284c7 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        letter-spacing: -0.025em;
      }
      .hero p {
        font-size: 1.25rem;
        margin: 1.5rem 0;
        color: var(--text-muted);
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
      }
      .container {
        max-width: var(--max-width);
        margin: 0 auto;
        padding: 4rem 2rem;
      }
      .section {
        margin-bottom: 4rem;
      }
      .section h2 {
        font-size: 2rem;
        margin-bottom: 2rem;
        color: #0f172a;
      }
      .notes-list {
        list-style: none;
        padding: 0;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
      }
      .note-item {
        background: white;
        padding: 2rem;
        border-radius: 12px;
        border: 1px solid var(--border-color);
        transition: all 0.2s ease;
        text-decoration: none;
        display: block;
      }
      .note-item:hover {
        border-color: var(--accent-color);
        box-shadow: 0 10px 30px -10px rgba(2, 132, 199, 0.15);
        transform: translateY(-2px);
      }
      .note-item h3 {
        margin: 0 0 0.75rem 0;
        font-size: 1.25rem;
        color: var(--text-color);
      }
      .note-item p {
        margin: 0;
        color: var(--text-muted);
        font-size: 0.9375rem;
      }
      .note-item .meta {
        display: flex;
        align-items: center;
        margin-top: 1.5rem;
        font-size: 0.8125rem;
        font-weight: 600;
        color: var(--accent-color);
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }
      @media (max-width: 640px) {
        .hero h1 {
          font-size: 2.5rem;
        }
        .notes-list {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>
  <body>
    <header class="header">
      <a href="/" class="logo">Suifeng Wudong</a>
      <nav class="nav-links">
        <a href="/">Home</a>
        <a href="/notes">Notes</a>
        <a href="/about">About</a>
      </nav>
    </header>

    <main>
      <div class="hero">
        <h1>Academic Notes</h1>
        <p>
          记录和分享各种技术笔记，目前采用 latex 生成 pdf 文件的形式进行展示。
        </p>
      </div>

      <div class="container">
        <section class="section">
          <h2>技术博客与优化</h2>
          <ul class="notes-list">
            <li>
              <a href="/blogs/powershell-optimization/" class="note-item">
                <h3>
                  PowerShell 启动卡顿诊断：Conda 初始化的 1.5s 延迟与优化方案
                </h3>
                <p>
                  通过将 Conda 初始化迁移至 PowerShell 7
                  并实施懒加载技术，成功将终端启动延迟从 2 秒缩短至 300 毫秒。
                </p>
                <div class="meta">查看详情 →</div>
              </a>
            </li>
            <li>
              <a href="/blogs/bilibili-dwld-idm/" class="note-item">
                <h3>破解 B 站视频下载限制：IDM 无法下载 DASH 流的解决方案</h3>
                <p>
                  深入分析 B 站 DASH 流的 M4S 文件结构，揭示其与传统 MP4
                  的区别，并提供让 IDM 继续下载的实用方法。
                </p>
                <div class="meta">查看详情 →</div>
              </a>
            </li>
          </ul>
        </section>

        <section class="section">
          <h2>学术笔记</h2>
          <ul class="notes-list">
            <li>
              <a href="/notes/operations-research/" class="note-item">
                <h3>运筹学：确定性方法</h3>
                <p>
                  线性规划、对偶理论、单纯形法以及整数规划的深度解析。包含大量的数学公式与算法实现。
                </p>
                <div class="meta">查看详情 →</div>
              </a>
            </li>
          </ul>
        </section>
      </div>
    </main>
  </body>
</html>
